WINEPREFIX="$HOME/.local/share/wineprefixes/resonance"

echo "WINEPREFIX=$WINEPREFIX wine $INSTALL_DIR/Resonance.exe" > $INSTALL_DIR/Resonance
echo "WINEPREFIX=$WINEPREFIX wine $INSTALL_DIR/winsetup.exe" > $INSTALL_DIR/winsetup
chmod u+x $INSTALL_DIR/Resonance
chmod u+x $INSTALL_DIR/winsetup

#edit acsetup.cfg to:
#gfxdriver=DX5

echo "To run, enter '$INSTALL_DIR' and type 'Resonance'."
